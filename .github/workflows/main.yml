name: uniq

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: install dependencies
        run: sudo apt update && sudo apt install -y emacs
      - name: Run tests
        run: test/test.sh
